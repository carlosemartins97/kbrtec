import React from 'react';

import {
    Container,

} from './styles';

const Divisor = ({children}) => {
    return (
        <Container>
            {children}
        </Container>
    );
};

export default Divisor;

