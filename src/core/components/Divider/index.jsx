import React from 'react';

import {
    Container,

} from './styles';

const Divider = ({children}) => {
    return (
        <Container>
            {children}
        </Container>
    );
};

export default Divider;

